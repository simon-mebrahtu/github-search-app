import { UserSearchResponse } from './user-search-response';

describe('UserSearchResponse', () => {
  it('should create an instance', () => {
    expect(new UserSearchResponse()).toBeTruthy();
  });
});
