import { RepositorySearchResponse } from './repository-search-response';

describe('RepositorySearchResponse', () => {
  it('should create an instance', () => {
    expect(new RepositorySearchResponse()).toBeTruthy();
  });
});
